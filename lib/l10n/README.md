# Note for Localization

The `app_localizations*.dart` files are **generated**. 

You only need to modify the `app_{language_code}.arb` files for the translations.

Read this reference for more detailed information:

https://docs.flutter.dev/ui/internationalization