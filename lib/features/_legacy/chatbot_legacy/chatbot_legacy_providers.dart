import 'package:provider/single_child_widget.dart';

/// App-wide blocs for the legacy markdown/citation chat pages.
///
/// The `chatbot` name now belongs to the AI Assistant surface in
/// `lib/features/chatbot/`. Screen-scoped blocs belong on their route.
class ChatbotLegacyProviders {
  static List<SingleChildWidget> get providers => [];
}
