import 'package:provider/single_child_widget.dart';

/// App-wide blocs for the AI Assistant surface.
///
/// Spread into the root `MultiBlocProvider` in `main.dart`. Screen-scoped blocs
/// belong on their route, not here.
class ChatbotProviders {
  static List<SingleChildWidget> get providers => [];
}
