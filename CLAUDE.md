# CLAUDE.md

The working rules are tool-agnostic and live in **[`AGENTS.md`](AGENTS.md)** (the shared standard other agents read too). Claude Code auto-loads this file and pulls that one in via the import below — so everything in AGENTS.md applies here.

@AGENTS.md

<!-- Add Claude-specific notes ONLY below (slash commands, Claude-only workflow tweaks). None so far. -->
